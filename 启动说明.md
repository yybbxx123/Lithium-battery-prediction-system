# 启动说明

本项目分两块：**前端可单独用**；**后端可选**，用于真实 DeepSeek 文案。

---

## 一、只跑前端（不调 AI）

1. 进入目录：`个人/frontend`
2. 用浏览器直接打开 `index.html`  
   或用任意静态服务，例如：

```bash
# Python 3
cd frontend
python -m http.server 5500
```

浏览器访问：`http://127.0.0.1:5500`

此时上传、预测、图表等本地逻辑可用；健康分析 / AI 助手在接口不可用时会用前端兜底文案。

---

## 二、启动 DeepSeek 后端（推荐）

### 1. 环境准备

- Python 3.10+ 建议
- 准备 DeepSeek API Key

### 2. 安装依赖

```bash
cd 个人/backend
pip install -r requirements.txt
```

### 3. 配置密钥

在 `backend` 目录创建或编辑 `.env`：

```env
DEEPSEEK_API_KEY=你的密钥
```

不要把真实密钥写进 README，也不要提交 `.env`。

### 4. 启动服务

```bash
cd 个人/backend
uvicorn app:app --reload --port 8000
```

成功后：

- 健康检查：`http://127.0.0.1:8000/api/health`
- 接口文档：`http://127.0.0.1:8000/docs`

### 5. 打开前端并对接后端

1. 打开 `frontend/index.html`（或静态服务地址）
2. 前端默认请求：`http://127.0.0.1:8000`
3. 若端口不同，可在浏览器控制台执行：

```js
window.BATTERY_API_BASE = "http://127.0.0.1:你的端口"
```

### 主要接口

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/health` | 服务存活检查 |
| POST | `/api/health-insight` | AI 健康分析 |
| POST | `/api/battery-chat` | AI 电池助手 |

---

## 三、为什么不是 `npm run dev`？

- 前端是静态 SPA，没有 `package.json` / Vite 构建链
- LLM 后端是 **Python FastAPI**，启动命令是 `uvicorn`，不是 npm
- 若只想「一条命令」，可自行在 `package.json` 里写脚本去调用 `uvicorn`，本质仍是起 Python 服务

---

## 四、常见问题

**Q：前端能开，但 AI 分析失败？**  
检查：后端是否在 8000 端口；`.env` 是否配置了 `DEEPSEEK_API_KEY`；浏览器控制台是否有 CORS / 网络错误。

**Q：不想装后端可以吗？**  
可以。前端会走本地兜底逻辑，页面其余功能不受影响。

**Q：单独测试模型调用？**  
可在 `backend` 目录运行：`python battery_llm.py`（需已配置密钥）。
